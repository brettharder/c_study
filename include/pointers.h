#ifndef POINTERS_H
#define POINTERS_H

// Phase 1: Pointer exercises

// Swap two integers via pointers
void swap(int *a, int *b);

// Multiply two numbers via pointers
void multiply_two_numbers(int *a, int *b);

// Demo function to run pointer exercises
void pointers_demo(void);

#endif // POINTERS_H
