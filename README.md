# C Project Template (macOS + clang + make)

## Commands
- `make run`       # debug build, runs app
- `make release`   # optimized build
- `make sanitize`  # ASan + UBSan build
- `make test`      # builds/runs tests
- `make bench`     # builds/runs benchmarks
- `make clean`     # removes build artifacts

test
